apiVersion: apps/v1
kind: Deployment
metadata:
  name: linkis-bml-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: linkis-bml
      # release: dev
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  
  template:
    metadata:
      labels:
        app: linkis-bml
        # release: dev
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: "app"
                    operator: In
                    values:
                      - linkis-bml
              topologyKey: "kubernetes.io/hostname"
      containers:
      - name: linkis-bml
        image: 10.206.0.5/library/linkis:linkis-bml
        lifecycle:
            preStop:
              exec:
                command: ["sh","/opt/ihome/linkis-bml/bin/stop.sh"]
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 14008
        env:
          - name: EUREKA_URL
            value: http://10.206.0.12:20230/eureka/
          - name: SERVER_HEAP_SIZE
            value: 1024M
          - name: START_PORT
            value: "14001"
        volumeMounts:
          - name: linkis-gateway-config
            mountPath: /opt/ihome/conf
      imagePullSecrets:
        - name: registry-key
      volumes:
        - name: linkis-bml-config
          configMap:
            name: linkis-bml-config
